/**
 * @file Renders the UI for the workflow review system.
 */
export declare class WorkflowReviewUI {
    private element;
    private currentWorkspaceId;
    private currentUserId;
    private workflowId;
    constructor(container: HTMLElement, workspaceId: string, userId: string, workflowId: string);
    private render;
    private handleMerge;
}
//# sourceMappingURL=workflow-review-ui.d.ts.map